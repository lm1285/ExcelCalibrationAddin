# Excel 校准加载项功能需求规格说明

本文档沉淀当前问答确认的业务规则，用于后续开发、重构和验收。若与现有实现冲突，以本文档作为目标需求，不代表当前代码已经全部实现。

## 1. 总体定位

本项目是用于 Excel 校准记录表的 VSTO 加载项。核心目标是：

1. 基于 Excel 打印区域识别校准模板结构。
2. 匹配本地模板库中的已同步模板。
3. 在用户确认或编辑模板后，将模板保存到远端，并由远端回传更新本地缓存。
4. 根据模板规则生成测量随机数，并写入测量值区域。

日常生成随机数应优先依赖本地模板库，远端只作为模板同步源，避免网络不可用影响现场使用。

## 2. 本地与远端模板库

### 2.1 模板同步模式

模板由用户上传至远端，加载项通过手动同步或定时同步下载到本地 SQLite 模板库。运行时优先使用本地模板。

同步支持：

- 手动同步。
- 每天自动同步一次。
- Excel 启动时检查上次同步时间，超过一天则后台同步。

第一阶段自动同步运行在 Excel 加载项内，因此只有 Excel 打开且加载项运行时才会触发。同步失败只提示并记录日志，不影响本地已有模板继续使用。

### 2.2 增量更新依据

远端模板至少需要提供：

- `template_id`：远端模板唯一 ID。
- `template_name`：模板名称。
- `version`：模板版本号。
- `updated_at`：远端更新时间。
- `status`：启用、停用、废止。
- `deleted_at` 或等价删除/废止标记。

同步优先使用版本号判断，其次使用更新时间。远端不允许两个同名模板；同名模板直接替换。

### 2.3 保存策略

模板保存默认仅保存远端。远端保存成功后，由远端回传模板数据，再写入本地缓存。

远端不可用时，模板先本地暂存，标记为待上传。下次同步成功后自动上传并转为启用状态。

### 2.4 状态

业务状态：

- 启用。
- 停用。
- 废止。

本地同步状态：

- 待上传。
- 冲突。
- 同步失败。

匹配到停用或废止模板都必须提示，不能静默忽略。

废止模板同步到本地后保留废止状态，不直接删除。

冲突时弹窗让用户选择：

- 保留本地。
- 使用远端。
- 另存。

暂不做登录和权限控制。

## 3. 识别模板流程

### 3.1 点击识别模板

目标流程：

```text
点击识别模板
  -> 快速扫描打印区域，生成模板指纹
  -> 本地模板匹配
    -> 强匹配命中：弹窗 查看 / 编辑 / 覆盖 / 另存 / 关闭
    -> 未命中：进入候选或新模板链路
  -> 远端不在运行时实时匹配，远端模板通过同步进入本地
```

侧边栏默认关闭，只在识别、查看、编辑、覆盖、另存时打开。

### 3.2 匹配成功弹窗

匹配成功后弹窗提供：

- 查看。
- 编辑。
- 覆盖。
- 另存。
- 关闭。

行为：

- 查看：打开侧边栏只读，展示侧边栏所有信息，允许点击字段高亮 Excel 区域，不允许修改。
- 编辑：不重新识别，基于已匹配模板规则编辑，保存后上传远端。
- 覆盖：重新识别当前 Excel，保存时覆盖同一个模板。
- 另存：重新识别当前 Excel，保存为新模板，必须输入模板名称。
- 关闭：不打开侧边栏。

### 3.3 匹配策略

强匹配条件：

- 指纹一致。
- 字段定位一致。
- 单位一致。
- 正负误差规则一致。

强匹配才允许自动命中并用于生成。

候选匹配：

- 表头或项目相似，但定位、单位或误差规则不完全一致。
- 只提示候选，不允许直接生成。
- 用户确认后可查看、另存或重新识别。

指纹不包含具体测量数据。修改测量值或标准值不应影响指纹；修改表头名称或区域布局可能影响指纹。

## 4. 识别范围与结构

### 4.1 范围

所有 Excel 模板都有打印区域。识别以打印区域为准。

### 4.2 校准项切分

优先级：

1. 项目标题块。
2. 表头字段。

示值误差、重复性、稳定性、漂移、响应时间、报警功能、报警动作值、外观等都可作为校准项。

外观等非数值项目只识别展示，保存模板时保留，生成随机数时跳过。

### 4.3 必填与可选字段

必填字段：

- 标准值。
- 测量值。
- 误差。
- 技术要求/允许误差。

可选字段：

- 平均值/AVG。
- 扩展不确定度。
- 结论。
- 量程。
- 报警限/动作值。
- 标气浓度值等上下文字段。

如果必填字段缺失，不允许保存模板，除非用户停用对应校准项。

### 4.4 字段识别词库

候选词库：

```text
校准项标题：
示值误差、基本误差、引用误差、重复性、稳定性、漂移、响应时间、报警功能、报警动作值、外观、绝缘电阻、零点漂移、量程漂移

标准值：
标准值、标准点、标称值、设定值、给定值、校准值、参考值、标准气浓度、标气浓度、标准浓度、标准输入、输入值

测量值：
测量值、实测值、读数、读值、示值、仪器示值、显示值、检测值、观测值、采样值、测得值

测量次数：
1、2、3、第1次、第2次、第3次、第一次、第二次、第三次、读数1、读数2、读数3

平均值：
AVG、Average、平均、平均值、均值、算术平均值

误差：
误差、示值误差、绝对误差、相对误差、引用误差、基本误差、重复性、稳定性、漂移

技术要求：
技术要求、允许误差、允差、最大允许误差、MPE、±MPE、限值、误差限、指标要求、报警动作值允许误差

量程：
量程、范围、测量范围、满量程、满程、满刻度、FS、Full Scale、Span

不确定度：
不确定度、扩展不确定度、U、Urel、urel、k=2

结论：
结论、判定、P/F、合格判定、结果判定
```

词库不命中时，仍应通过标题形态、父子表头、连续数字列和数据形态进行兜底识别，但置信度较低。低置信度结果在侧边栏标记为待确认。

## 5. 合并单元格、多级表头与行级映射

合并单元格、一二级表头是常态。识别时必须把合并单元格当作一个逻辑单元。

一个校准项可能包含多个标准值，例如 10、30、50。通常：

- 标准值纵向排列。
- 同一标准值的一组测量值在同一行横向排列。
- 每个标准值行对应一个误差。
- 技术要求可能是一个纵向合并区域，覆盖多个标准值行。
- 扩展不确定度、结论等也可能是合并区域。

识别结果必须建立行级映射：

```text
校准项
  标准值行
    标准值单元格
    测量值单元格组
    平均值单元格
    误差单元格
    技术要求单元格或合并区域
    不确定度单元格或合并区域
    结论单元格
```

技术要求合并区域按覆盖范围分配。标准值行落在哪个技术要求合并区域内，就使用哪个技术要求。如果技术要求覆盖整个项目，则所有标准值共用该技术要求。

标准值为空的行不生成随机数。该行已有测量值保持不动；后续用户填入标准值后再次生成，应能正常生成。

`标气浓度值` 这类字段是某些模板中的上下文字段，不是固定必备字段。

## 6. 单位与误差识别

### 6.1 单位来源优先级

1. 表头。
2. 单元格格式。
3. 单元格内容。

用户会保证：

- 测量值单位等于标准值单位。
- 误差单位等于技术要求单位。

因此第一阶段不做复杂单位换算。

### 6.2 误差写法

必须支持：

- `±0.5`
- `±0.5%FS`
- `±0.5%`
- 绝对误差写法中，`±0.5` 后方可能跟随单位，例如 `±0.5 µmol/mol`、`±2 ℃`
- `<`
- `≤`
- `>`
- `≥`

不会有正负不对称误差。

单边指标如重复性、稳定性、漂移等也按上下波动生成，最后取误差绝对值并满足技术要求。

## 7. 公式定位

识别模板时需要记录：

- 平均值公式。
- 误差公式。
- 结论公式。
- 技术要求公式。

技术要求公式可能是简单条件判断，例如：

```excel
=IF($Z$6<=10,"±2µmol/mol","±10%")
```

模板保存时需要保存公式引用关系。生成时读取当前工作簿对应单元格，先计算公式结果，再解析为 MPE。

重复性、稳定性、漂移等算法不统一，不能只靠固定公式模板。应记录公式引用关系和字段定位，生成时以公式计算结果作为验证依据。

如果误差公式存在，生成随机数时以误差公式计算结果为准验证生成结果。

如果误差公式不存在，但误差区域有历史数值，不参考历史误差，只按技术要求直接生成。

## 8. 生成随机数

### 8.1 入口规则

点击生成随机数时，只使用已匹配模板。不做完整识别。没有模板命中时，提示用户先执行识别模板。

### 8.2 写入规则

- 默认覆盖已有测量值。
- 标准值为空的行不生成，已有测量值保持不动。
- 只写测量值父表头下的数字列，如 1、2、3。
- 不写 AVG/平均值、误差、结论。
- AVG/平均值、误差、结论如果没有公式，不由插件写入，只提示用户。
- 整个校准项失败时整项不写入，并提示原因。
- 停用校准项跳过不生成。
- 非数值项目跳过不生成。

生成后必须满足误差绝对值小于技术要求。重复性、稳定性等单边指标也必须满足最终绝对误差小于技术要求。

同一标准值内多次测量值应接近。同一校准项内多个标准值的误差方向尽量一致。

## 9. 随机数配置

配置优先级：

```text
一次性覆盖 > 模板独立配置 > 全局配置
```

配置规则：

- 生成目标误差按技术要求占比控制。
- 例如 `±10%`，配置 `30%~70%`，目标误差落在 `±3%~±7%`。
- `%FS` 先解析为最终 MPE，再乘配置系数。
- `≤0.5`、`＜0.5` 等上限按配置比例生成，例如 `30%~70% * 上限`。
- 偏差方向支持：自动随机、同一校准项保持同方向、强制正偏差、强制负偏差。
- 同一标准值内多次测量值波动按误差百分比控制。
- 多个标准值之间误差趋势波动也按误差百分比控制，比例可调。
- 小数位默认按模板/单元格格式。Ribbon 一次性小数位只影响本次。
- 分布支持正态分布、均匀分布、三角分布。
- 业务文案对应：常规测量、均匀试探、人工微调。
- 默认使用常规测量。
- 预览生成结果可选，默认不做。

## 10. 侧边栏与区域设置

侧边栏展示：

- 项目名称。
- 启用状态。
- 标准值区域。
- 测量值区域。
- 误差区域。
- 技术要求区域。
- 量程区域。
- 不确定度区域。
- 结论区域。
- 行级映射状态。
- 公式定位状态。
- 随机数配置相关信息。

区域设置支持：

- 点击字段高亮 Excel 区域。
- 一键录入模式：点击字段旁取选区后，用户在 Excel 框选区域即可自动写入该字段。
- 连续录入模式：按标准值、测量值、误差、技术要求等顺序连续指定区域。
- 右键或快捷菜单：选中 Excel 区域后可设为当前校准项的标准值、测量值、误差、技术要求等区域。
- 区域质量提示：选完后立即提示行数是否匹配、是否包含公式、是否包含合并单元格、是否与标准值行对齐。
- 复制上一项结构：用于多个相似校准项快速套用区域布局。

保存模板前，如果必填字段缺失，必须阻止保存并列出缺失项。

## 11. 错误提示、日志与诊断

用户可见错误提示使用业务语言，不展示异常堆栈。

日志需要记录：

- 工作簿名。
- 工作表名。
- 模板名。
- 指纹。
- 匹配来源。
- 本地/远端或同步耗时。
- 规则数。
- 失败原因。

识别失败时，也打开侧边栏，展示已识别到的部分，并标出缺失字段。

生成失败时，需要定位到失败校准项，并高亮对应区域。

同步失败时只轻提示并记录日志，不打断当前 Excel 操作。

需要提供导出诊断包功能，包含：

- 日志。
- 模板指纹。
- 识别结果 JSON。
- 模板匹配结果。
- 必要的运行环境信息。

日常诊断记录只保存摘要，不保存完整单元格快照。摘要包含指纹、字段区域、行级映射、公式定位、解析结果和失败原因。诊断摘要和日志应异步写入，不阻塞识别和生成主流程。

## 12. 性能与生成路径约束

使用范围限于 Excel 打印区域内。

点击生成随机数后的目标性能：

- 小模板：100ms 内。
- 常规模板：300ms 内。
- 大模板：不超过 1s，并显示进度。

为满足性能目标，生成路径必须遵守：

- 只使用本地已缓存模板。
- 不访问远端。
- 不做完整模板识别。
- 不扫描全表。
- 只读取模板保存的固定区域。
- Excel COM 读取和写入必须批量化，禁止逐单元格频繁访问。
- 随机数计算在内存中完成。
- 测量值区域一次性批量写入。
- 日志和诊断异步写入。

校验分为两层：

- 模板保存/同步时做重校验：字段完整性、公式定位、单位、误差规则、行级映射、必填字段等。
- 点击生成时只做轻校验：当前工作簿指纹/定位是否一致、标准值是否为空、目标单元格是否可写、技术要求公式结果是否可读。

## 13. 模块边界与重构规划

当前项目已经存在文件和功能过于庞大的风险。后续开发必须按模块边界推进，禁止继续向大文件追加复杂业务。

### 13.1 当前高风险文件

当前需要重点控制的文件：

- `MeasurementRuleDraftBuilder.cs`：约 2178 行，约 101 个方法。混合了项目块识别、字段区域推断、表头识别、重复性识别、误差列推断、合并单元格处理和规则草稿构建。
- `CalibrationTaskPaneControl.cs`：约 1652 行，约 104 个方法。混合了 UI 布局、侧边栏状态、区域编辑、模板保存、校准项增删、只读/编辑状态和模型克隆。
- `GenerateMeasurementUseCase.cs`：约 1240 行，约 84 个方法。混合了生成流程、规则校验、参数解析、可写单元格解析、随机数生成和写入前准备。
- `MeasurementRuleParameterResolver.cs`：约 1105 行，约 44 个方法。混合了标准值解析、技术要求解析、量程解析、误差类型识别、单位/百分号判断和公式上下文处理。
- `ThisAddIn.cs`：约 879 行，约 44 个方法。混合了 VSTO 生命周期、Excel 事件、自动匹配、生成入口、状态缓存、配置刷新和侧边栏控制。

这些文件后续只能做小范围修复。新增功能不得继续堆入这些文件，除非是为了迁出逻辑或保持兼容的薄入口。

### 13.2 推荐目标模块

#### 识别模板

```text
Host/Recognition
  PrintAreaSnapshotService
  TemplateFingerprintMatcher
  CalibrationSectionDetector
  FieldHeaderDetector
  UnitResolver
  RequirementTextParser
  RowMappingBuilder
  MergedCellLogicalRangeResolver
  FormulaReferenceAnalyzer
  RecognitionDiagnosticsBuilder
```

职责：

- `PrintAreaSnapshotService`：只负责基于打印区域生成识别输入。
- `TemplateFingerprintMatcher`：只负责强匹配和候选匹配判断。
- `CalibrationSectionDetector`：只负责按项目标题块切分校准项。
- `FieldHeaderDetector`：只负责字段表头和父子表头识别。
- `UnitResolver`：只负责表头、格式、内容中的单位提取。
- `RequirementTextParser`：只负责 `±`、百分比、`%FS`、上下限等技术要求文本解析。
- `RowMappingBuilder`：只负责标准值行到测量值、误差、技术要求等区域的行级映射。
- `MergedCellLogicalRangeResolver`：只负责合并单元格逻辑区域。
- `FormulaReferenceAnalyzer`：只负责平均值、误差、结论、技术要求公式引用关系。
- `RecognitionDiagnosticsBuilder`：只负责诊断摘要，不参与业务判断。

#### 模板库与同步

```text
Host/Templates
  TemplateSaveService
  TemplateSyncService
  TemplateDownloadPlanner
  PendingUploadQueue
  TemplateConflictResolver
  TemplateStatusPolicy

Core/TemplateCache
  LocalTemplateRepository
  TemplateSyncMetadataRepository
```

职责：

- 保存模板默认走远端，远端回传后写本地。
- 远端不可用时写入 `PendingUploadQueue`。
- 同步状态和业务状态分开管理。
- 冲突处理独立在 `TemplateConflictResolver`。

#### 生成随机数

```text
Host/Generation
  GenerationPreflightValidator
  GenerationPlanBuilder
  StandardRowValueReader
  RequirementValueResolver
  MeasurementSeriesGenerator
  FormulaResultVerifier
  GenerationFailureReporter
```

职责：

- `GenerationPreflightValidator`：点击生成时只做轻校验。
- `GenerationPlanBuilder`：把模板规则转成可批量读取和写入的计划。
- `StandardRowValueReader`：只读取标准值和必要上下文。
- `RequirementValueResolver`：解析技术要求公式结果和 MPE。
- `MeasurementSeriesGenerator`：只负责内存中的随机数生成。
- `FormulaResultVerifier`：按误差公式或技术要求验证结果。
- `GenerationFailureReporter`：生成业务化失败原因。

#### Excel 适配

```text
Host/ExcelInterop
  ExcelPrintAreaReader
  ExcelRangeBatchReader
  ExcelRangeBatchWriter
  ExcelSelectionService
  ExcelHighlightService
```

职责：

- 所有 Excel COM 访问必须集中在 ExcelInterop。
- 业务模块只使用快照、区域模型和批量读写结果。
- 禁止业务服务直接访问 Excel COM。

#### 侧边栏 UI

```text
Vsto/TaskPane
  CalibrationTaskPaneControl
  TaskPaneStateStore
  CalibrationItemListView
  FieldRegionEditor
  RowMappingView
  TemplateActionController
  SyncStatusView
```

职责：

- `CalibrationTaskPaneControl` 只保留控件装配和事件转发。
- `TaskPaneStateStore` 管理侧边栏状态。
- `CalibrationItemListView` 只负责校准项列表展示。
- `FieldRegionEditor` 只负责字段区域编辑交互。
- `RowMappingView` 展示行级映射。
- `TemplateActionController` 处理查看、编辑、覆盖、另存、保存。
- `SyncStatusView` 展示待上传、冲突、同步失败。

#### VSTO 入口

```text
Vsto
  ThisAddIn
  AddinLifecycleController
  WorkbookEventRouter
  TaskPaneController
  RibbonCommandController
  GenerationStateCache
  DailySyncScheduler
```

职责：

- `ThisAddIn` 只保留 VSTO 生命周期入口和依赖装配。
- Excel 事件转发到 `WorkbookEventRouter`。
- 侧边栏显示隐藏交给 `TaskPaneController`。
- Ribbon 命令交给 `RibbonCommandController`。
- 每日同步交给 `DailySyncScheduler`。

### 13.3 拆分顺序

后续重构按以下顺序推进：

1. 新增目标目录和只读模型，不改行为。
2. 从 `MeasurementRuleDraftBuilder` 拆出合并单元格逻辑和行级映射构建。
3. 从 `MeasurementRuleParameterResolver` 拆出技术要求文本解析和单位解析。
4. 从 `GenerateMeasurementUseCase` 拆出轻校验、生成计划和公式验证。
5. 从 `CalibrationTaskPaneControl` 拆出字段区域编辑和模板操作控制。
6. 从 `ThisAddIn` 拆出事件路由、状态缓存和同步调度。

每次只迁出一小块逻辑，并保持行为不变。每拆一块必须构建或运行对应测试。

## 14. 全局防膨胀规则

后续开发必须遵守以下规则，防止再次出现单文件或单功能拖累整个插件。

### 14.1 文件规模

- 普通业务类目标控制在 300 行以内。
- 复杂编排类最多 500 行。
- UI 控件类最多 500 行，超过必须拆子控件或控制器。
- 单文件超过 500 行时，新增功能前必须先评估拆分点。
- 单文件超过 800 行时，除修复缺陷外禁止继续新增业务逻辑。

### 14.2 方法规模

- 单个方法目标控制在 40 行以内。
- 超过 80 行必须拆分。
- 一个方法只做一个动作：识别、解析、校验、生成、写入、展示不能混在一起。

### 14.3 职责边界

- UI 不写业务规则，只展示状态和收集用户输入。
- Host 负责编排流程和业务用例。
- Core 放不依赖 Excel COM 和 UI 的纯逻辑。
- Excel COM 访问只允许出现在 ExcelInterop 适配层。
- 模板同步不直接依赖生成逻辑。
- 生成逻辑不直接依赖远端。
- 诊断日志不影响主流程。

### 14.4 新功能准入

新增功能前必须先回答：

1. 这个功能属于识别、模板库、同步、生成、Excel 适配、UI 中哪一层？
2. 是否需要新模型或新服务，而不是塞进现有大类？
3. 是否会影响生成 100ms/300ms/1s 性能目标？
4. 是否有可测试的纯逻辑部分？
5. 失败时用户看到什么业务提示，日志记录什么技术细节？

如果无法回答，不能直接写代码。

### 14.5 性能边界

- 生成路径不得做完整识别。
- 生成路径不得访问远端。
- 生成路径不得扫描全表。
- 生成路径不得逐单元格频繁访问 Excel COM。
- 重校验放在模板保存/同步阶段。
- 生成时只做轻校验和批量读写。

### 14.6 测试边界

以下纯逻辑必须有单元测试或最小复现数据：

- 技术要求文本解析。
- 单位提取。
- `%FS` 和绝对误差解析。
- 合并单元格逻辑区域。
- 行级映射。
- 模板强匹配和候选匹配。
- 随机数生成范围。
- 公式结果验证。

### 14.7 禁止事项

- 禁止把新的识别规则继续堆进 `MeasurementRuleDraftBuilder`。
- 禁止把新的随机数规则继续堆进 `GenerateMeasurementUseCase`。
- 禁止把新的侧边栏状态和业务判断继续堆进 `CalibrationTaskPaneControl`。
- 禁止在 UI 事件中直接访问数据库、远端 API 或 Excel COM 批量以外的单元格循环。
- 禁止在生成按钮点击后做远端同步、完整识别或全表扫描。

## 15. 开发优先级建议

1. 建立稳定验证基线：构建脚本、平台配置、本地服务锁文件问题、基础测试。
2. 补充模板同步模型：远端保存、回传本地、待上传、冲突、同步失败状态。
3. 重构识别流程：快速指纹匹配与完整识别分离。
4. 实现行级映射模型，支持多标准值、多级表头和合并单元格。
5. 完善公式定位与 MPE 解析。
6. 完善侧边栏只读/编辑/覆盖/另存流程。
7. 完善随机数生成配置和验证。
8. 增加诊断包导出。
