# Excel 校准加载项项目技术文档

> 更新时间：2026-07-26  
> 文档状态：最终整合版。  
> 文档定位：面向开发、维护、审查和交接的唯一技术总览，整合当前架构、核心流程、开发进度、2026-07-26 项目审查结论、验证证据、风险和后续优先级。

## 1. 项目定位

本项目是一个用于 Excel 校准表的 VSTO 加载项。它读取当前工作簿结构，识别校准模板和校准项目区域，匹配本地或远端模板规则，解析标准值、误差、量程、公式等参数，再生成符合约束的测量随机数并写回 Excel。

项目当前已经不是单纯的“随机填数工具”，而是围绕“模板识别、规则沉淀、参数解析、预览校验、生成写回、本地缓存、远端同步”形成的一套 Excel 校准模板工作流。

核心目标：

1. 降低校准表人工填数和复核成本。
2. 将不同 Excel 模板的字段、区域、公式和生成规则沉淀为可复用模板。
3. 通过本地 SQLite 缓存和远端模板库匹配，提高模板复用率。
4. 在不破坏 Excel 原始公式和格式的前提下，完成可追踪、可回滚的测量值写入。

## 2. 技术栈与运行环境

| 项目 | 当前选择 |
| --- | --- |
| 主语言 | C# |
| 主框架 | .NET Framework 4.8 |
| Excel 集成 | VSTO / Excel COM Interop |
| UI | WinForms + VSTO Ribbon + CustomTaskPane |
| 本地缓存 | SQLite，依赖 `System.Data.SQLite.Core` |
| JSON | `Newtonsoft.Json` |
| 本地服务 | `HttpListener` loopback HTTP 服务 |
| 测试框架 | MSTest |
| 解决方案 | Visual Studio 2022 `.sln` |

运行前置条件：

1. Windows 桌面版 Excel。
2. Visual Studio 2022，包含 Office/SharePoint 开发工具。
3. .NET Framework 4.8 targeting pack。
4. Visual Studio Tools for Office Runtime。

## 3. 解决方案结构

```text
ExcelCalibrationAddin.sln
├─ src
│  ├─ ExcelCalibrationAddin.Contracts
│  ├─ ExcelCalibrationAddin.Core
│  ├─ ExcelCalibrationAddin.Host
│  ├─ ExcelCalibrationAddin.Vsto
│  └─ ExcelCalibrationAddin.LocalServer
├─ tests
│  └─ ExcelCalibrationAddin.Core.Tests
├─ tools
│  ├─ verify.ps1
│  ├─ install_debug_addin.ps1
│  ├─ reset_registration.ps1
│  └─ start_local_backend.ps1
├─ README.md
├─ 功能需求规格说明.md
└─ 项目技术文档.md
```

文档职责：

- `项目技术文档.md`：当前技术事实和项目状态的主文档。
- `功能需求规格说明.md`：目标业务规则和验收依据；目标需求不代表全部已经实现。
- `README.md`：环境准备、构建、测试、启动和安装的快速入口。

### 3.1 `ExcelCalibrationAddin.Contracts`

职责：定义跨层共享的数据模型和契约，保持为纯模型层。

关键内容：

- `WorkbookSnapshot`、`SheetSnapshot`、`CellMeta`：Excel 工作簿快照。
- `TemplateFingerprint`：模板指纹，用于本地和远端模板匹配。
- `RecognizedField`、`TemplateRegionMapping`：识别出的字段和区域映射。
- `MeasurementRule`：测量值生成规则，是识别、保存、生成的核心载体。
- `MeasurementGenerationInput` / `MeasurementGenerationResult`：生成器输入输出。
- `TemplateFieldDefinition`：模板字段定义，记录表头、区域、单位、公式和条件分支。

当前状态：

- 已覆盖快照、模板、规则、生成结果、生命周期、同步状态等主要数据结构。
- `Models.cs` 仍承载较多模型，后续可按领域拆分为工作簿、模板、生成、同步等文件。
- 新增的 `TemplateDefinitions.cs` 表明项目已开始固化更细粒度的模板结构，而不只依赖简单区域映射。

### 3.2 `ExcelCalibrationAddin.Core`

职责：承载不依赖 Excel COM 和 UI 的核心能力，是最适合自动化测试的一层。

关键模块：

- `MeasurementValueGenerator`：按标准值、误差、分布、方向、精度和边界生成测量值。
- `MeasurementSeriesGenerator`：处理多测量点、多标准值之间的序列关系和波动控制。
- `TemplateFingerprintBuilder`：根据工作簿快照生成稳定模板指纹。
- `FieldMatcher`：识别表格中的校准字段和项目区域。
- `LocalTemplateRuleCacheRepository`：SQLite 模板缓存读写、匹配、冲突处理、同步状态维护。
- `TemplateSyncClient`：访问模板后端 HTTP API。
- `ConfigurationLoader` / `GenerationConfigurationStore`：配置读取、默认值恢复、生成配置克隆和兼容迁移。
- `ParameterValueParser` / `NumberFormatInterpreter`：解析数值、百分号、单位和 Excel 数字格式。

当前状态：

- 生成算法、指纹、配置、缓存、同步客户端等核心能力已落在 Core 层。
- 已有大量测试覆盖核心行为。
- SQLite 连接串、配置默认值、旧字段迁移和请求安全边界已做过专项修正。

### 3.3 `ExcelCalibrationAddin.Host`

职责：连接 Core 能力、Excel 适配和 VSTO UI，是业务流程编排层。

关键模块：

- `PluginBootstrapper`：手工组装配置、HTTP 客户端、缓存、识别、同步、保存和生成用例。
- `PluginWorkflowOrchestrator`：对 VSTO 暴露识别匹配和生成写入入口。
- `TemplateRecognitionUseCase`：捕获工作簿快照，生成指纹，识别字段。
- `SyncTemplateUseCase`：解析远端匹配结果，支持远端/本地同步。
- `GenerateMeasurementUseCase`：解析规则、生成预览、写回 Excel。
- `MeasurementRuleDraftBuilder`：从识别结果和工作簿结构构建模板规则草稿。
- `MeasurementRuleParameterResolver`：解析标准值、技术要求、误差、量程、公式条件分支等参数。
- `MeasurementRuleStructureAnalyzer`：分析规则结构、公式引用和补充公式。
- `TemplateFieldDefinitionBuilder` / `TemplateFieldDefinitionMatcher`：构建和匹配模板字段定义。
- `ExcelInteropSnapshotProvider`：从 Excel COM 捕获快照。
- `ExcelInteropWriter`：向 Excel 写入生成值。
- `VstoAddinFacade`：给 VSTO 层提供更稳定的门面接口。

当前状态：

- Host 层已拆出较多独立服务，避免所有逻辑集中在 VSTO UI。
- `GenerateMeasurementUseCase.PreviewWorkflow.cs` 仍是当前最明显的维护热点，已从 1014 行降至 773 行；格式/分辨力与趋势误差职责已拆到独立 partial 文件。
- 模板字段定义、公式解析、单位解析、自动匹配工作表选择等能力已经开始模块化。

### 3.4 `ExcelCalibrationAddin.Vsto`

职责：Excel 加载项入口、Ribbon、任务窗格、对话框和 Excel 事件管理。

关键模块：

- `ThisAddIn`：VSTO 生命周期、启动初始化、Excel 事件路由、任务窗格生命周期、生成状态。
- `CalibrationRibbon`：Ribbon 按钮入口。
- `CalibrationTaskPaneControl`：侧边栏 UI，展示模板识别、校准项、规则编辑、模板保存和生成操作。
- `RandomGenerationConfigurationDialog`：随机数生成参数配置。
- `TemplateLibraryManagerDialog`：模板库管理、同步和诊断入口。
- `DailySyncScheduler`：启动后按日同步模板。
- `ExcelGenerationTransaction`：生成写入时的 Excel 状态保护和回滚边界。

当前状态：

- `ThisAddIn` 和 `CalibrationTaskPaneControl` 已通过 partial 文件按职责拆分。
- UI 层仍直接承担部分规则编辑状态管理，后续应继续把可测试的状态转换下沉到 Host。
- VSTO 和 Excel COM 相关行为必须通过真实 Excel 回归确认。

### 3.5 `ExcelCalibrationAddin.LocalServer`

职责：提供本地模板 HTTP 服务，复用同一个 SQLite 缓存，方便加载项通过 HTTP API 访问模板能力。

接口：

- `GET /api/excel-templates/health`
- `GET /api/excel-templates/list`
- `POST /api/excel-templates/match`
- `POST /api/excel-templates/save`

安全和边界：

- 只允许 HTTP loopback 地址。
- POST 接口强制 `application/json`。
- 请求体限制为 5 MB。
- 拒绝带 `Origin` 的浏览器来源请求。
- 内部异常不直接回传给客户端，避免泄露本地路径和堆栈信息。

当前状态：

- 已适合单机 Excel 加载项的本地模板服务场景。
- 仍是串行请求模型，缺少优雅停止、端口占用诊断和多客户端并发处理。

### 3.6 `ExcelCalibrationAddin.Core.Tests`

职责：覆盖 Core 与部分 Host 纯逻辑行为。

当前覆盖范围包括：

- 模板指纹稳定性和兼容匹配。
- 工作簿结构偏移、表头变化、公式结果变化的指纹处理。
- 模板字段定义、公式分支、单位和技术要求解析。
- 随机数生成边界、精度、趋势、方向和波动控制。
- 重复性、最大误差、报警值、手动标准值等业务分支。
- 模板保存、远端同步、本地缓存冲突和 pending upload。
- 行映射、合并单元格、可写单元格和公式验证。

当前状态：

- 已有 110 个测试通过的验证记录。
- 测试主要集中在 `CoreBehaviorTests.cs`，当前为 3777 行；新增预览工作流测试已放入独立领域文件，后续继续迁移。

## 4. 核心业务流程

### 4.1 启动初始化

```text
Excel 启动加载项
  -> ThisAddIn_Startup
  -> 读取 appsettings.json
  -> ConfigurationLoader 恢复默认值和兼容旧配置
  -> PluginBootstrapper 组装依赖
  -> VstoAddinFacade 暴露门面
  -> 初始化 Ribbon / TaskPane / 事件路由 / 每日同步调度
```

关键思路：

- VSTO 只负责 Excel 生命周期和 UI 事件。
- 业务入口统一收敛到 `VstoAddinFacade` 和 `PluginWorkflowOrchestrator`。
- 配置和服务依赖在启动阶段完成组装，运行时不在 UI 中临时拼业务对象。

### 4.2 模板识别与匹配

```text
用户打开或切换工作簿
  -> ExcelInteropSnapshotProvider 捕获当前工作簿快照
  -> TemplateFingerprintBuilder 生成模板指纹
  -> FieldMatcher 识别校准字段
  -> LocalTemplateRuleCacheRepository 本地精确/兼容匹配
  -> 本地无可用规则时调用 SyncTemplateUseCase 远端匹配
  -> 返回 RecognitionAndSyncResult
  -> TaskPane 展示匹配模板、识别字段和可编辑规则
```

关键思路：

- 优先使用本地缓存，减少远端依赖。
- 指纹应忽略测量值、标准值等易变数据，保留表头、结构和规则相关特征。
- 工作表切换、多工作簿并存和活动工作表过滤需要真实 Excel 回归保护。

### 4.3 模板草稿与保存

```text
识别结果 / 用户选择区域
  -> MeasurementRuleDraftBuilder 构建规则草稿
  -> TemplateFieldDefinitionBuilder 固化字段、单位、公式、条件分支
  -> TemplateRulePersistencePreparer 清理当前工作簿中的瞬时值
  -> TemplateSaveService 尝试远端保存
  -> 远端成功：写入本地 SQLite 为已同步模板
  -> 远端失败或响应不完整：写入 pending upload
```

关键思路：

- 模板保存的对象应是结构和规则，不应保存当前工作簿里的临时测量值。
- 保存前必须校验指纹、规则、测量区域、误差参数和模板身份。
- 远端不可用时不能丢失用户编辑结果，应进入本地待上传状态。

### 4.4 测量值生成与写回

```text
用户点击生成
  -> 从本地/远端/手动编辑规则得到 MeasurementRule
  -> MeasurementRuleParameterResolver 解析标准值、MPE、技术要求、量程和公式
  -> MeasurementRuleStructureAnalyzer 判断公式引用、误差类型和行映射
  -> GenerationRuleValidator 校验规则完整性
  -> WritableCellResolver 确定可写单元格
  -> MeasurementValueGenerator / MeasurementSeriesGenerator 生成测量值
  -> FormulaResultVerifier 校验公式回代结果
  -> ExcelInteropWriter 批量写回 Excel
```

关键思路：

- 先解析参数，再生成数值，最后回代验证。
- 对公式单元格、合并单元格、最大误差、重复性、报警值和手动标准值等分支要显式处理。
- 写入 Excel 时需要减少 COM 循环访问，优先批量读写，并通过事务对象保护 Excel 状态。

### 4.5 本地模板 HTTP 服务

```text
start_local_backend.ps1
  -> ExcelCalibrationAddin.LocalServer
  -> ConfigurationLoader 读取配置
  -> LocalTemplateRuleCacheRepository 初始化 SQLite
  -> HttpListener 监听 http://localhost:3002/
  -> /health /list /match /save
```

关键思路：

- 这是加载项本地辅助服务，不是多用户后端。
- 它的安全边界是 loopback、JSON、请求体限制和拒绝浏览器来源。
- 如果未来要支持多客户端，应迁移到正式 Web 宿主并补并发、鉴权和生命周期管理。

## 5. 关键数据模型

### 5.1 工作簿快照

`WorkbookSnapshot` 由多个 `SheetSnapshot` 组成，`SheetSnapshot` 包含 `CellMeta` 和 `HeaderPath`。快照记录文本、原始值、显示值、数字格式、合并区域、边框、背景色、公式、行高和列宽。

作用：

- 给指纹生成提供稳定输入。
- 给字段识别和模板草稿构建提供结构信息。
- 避免业务层直接依赖 Excel COM 对象。

### 5.2 模板指纹

`TemplateFingerprint` 包含：

- `ExactFingerprint`
- `FuzzyFingerprint`
- `StructureSignature`
- `Summary`
- `SheetNames`
- `Title`
- `HeaderTexts`

作用：

- 精确匹配本地模板。
- 兼容旧模板和结构相近模板。
- 区分表头、结构、技术要求变化。

### 5.3 测量规则

`MeasurementRule` 是生成链路的核心模型，包含：

- 目标区域和可写单元格。
- 标准值、平均值、误差、MPE、量程、不确定度、结果等参数来源。
- 行映射、手动标准值、固定容差、正负方向限制。
- 格式规则、分布模式、误差公式和模板字段定义。
- 生成参数覆盖。

作用：

- 连接模板识别、规则编辑、模板保存和生成写回。
- 将 Excel 区域、业务参数和生成策略放在同一个可序列化模型中。

### 5.4 模板字段定义

`TemplateFieldDefinition` 记录：

- 项目名称和项目区域。
- 多级表头。
- 各类区域角色。
- 单位来源和数字格式。
- 技术要求操作符和值。
- 公式引用和条件分支。

作用：

- 比传统区域映射更稳定地描述模板结构。
- 支持公式变化检测、单位识别和动态参数恢复。
- 为模板长期复用和版本演进提供结构化基础。

## 6. 配置与数据存储

配置文件：

- 示例：`appsettings.example.json`
- VSTO 运行配置：`src/ExcelCalibrationAddin.Vsto/appsettings.json`

关键配置：

```json
{
  "Backend": {
    "BaseUrl": "http://localhost:3002",
    "TemplateApiPrefix": "/api/excel-templates"
  },
  "Cache": {
    "SqliteFile": "%LOCALAPPDATA%\\ExcelCalibrationAddin\\cache.db"
  },
  "Generation": {
    "DefaultDistribution": "Normal",
    "ResultCalculationMethod": "FormulaBackCalculation",
    "StandardValueReference": "RecognizedStandardValueRange"
  }
}
```

本地数据：

- SQLite 默认路径：`%LOCALAPPDATA%\ExcelCalibrationAddin\cache.db`
- 主要保存模板指纹、模板名称、远端版本、生命周期状态、同步状态、目录元数据、规则 JSON 和生成配置 JSON。

日志与诊断：

- `AddinFileLogger` 用于加载项和本地服务日志。
- 模板库窗口提供诊断包导出入口。

## 7. 当前开发进度

| 模块 | 进度判断 | 说明 |
| --- | --- | --- |
| 解决方案分层 | 基本完成 | Contracts/Core/Host/Vsto/LocalServer/Tests 已拆分。 |
| VSTO 加载项入口 | 基本可用 | 生命周期、Ribbon、任务窗格、事件路由已存在，需真实 Excel 全流程回归。 |
| 工作簿快照捕获 | 基本可用 | 已支持文本、格式、公式、合并单元格等元数据，COM 行为仍需实机验证。 |
| 模板指纹 | 基本可用 | 已有测试覆盖忽略测量值、结构整体偏移、表头变化等场景。 |
| 字段识别与模板草稿 | 持续完善中 | 已有字段匹配、区域构建、单位和公式解析，新模板形态仍需样本扩展。 |
| 模板字段定义 | 新增能力，持续完善中 | 已能记录表头、区域、单位、公式分支，并参与参数恢复和匹配。 |
| 本地模板缓存 | 基本可用 | SQLite 读写、匹配、冲突、pending upload 和同步状态已有测试。 |
| 远端模板同步 | 基本可用 | HTTP 客户端、远端匹配、保存和同步结果解析已具备，需对接真实远端环境持续验证。 |
| 本地 HTTP 服务 | 单机场景可用 | loopback、安全限制和基础接口已完成，生命周期和并发能力待增强。 |
| 随机数生成 | 基本可用 | 已覆盖正负误差、相对误差、上限要求、重复性、最大误差、精度、趋势等分支。 |
| Excel 写回 | 基本可用 | 已有写入和事务封装，真实 Excel 中的撤销、异常回滚、关闭释放仍是重点风险。 |
| UI 交互 | 可用但偏重 | 任务窗格和对话框功能较完整，但部分规则状态仍耦合在 WinForms 控件中。 |
| 自动化测试 | 覆盖较多但结构需整理 | 已有 110 个通过记录；预览工作流测试已开始按领域文件拆分。 |
| 文档 | 已整合 | 本文档为当前技术主文档；README、需求规格、审查报告和重构路线各自承担快速入口、目标规则和历史追溯职责。 |

## 8. 当前维护热点

1. `tests/ExcelCalibrationAddin.Core.Tests/CoreBehaviorTests.cs` 当前 3777 行，测试覆盖较多但结构仍过于集中。
2. `src/ExcelCalibrationAddin.Host/UseCases/GenerateMeasurementUseCase.PreviewWorkflow.cs` 当前 773 行，仍是生成预览链路的最大业务热点；`PreviewFormatting` 和 `TrendErrors` 已先行拆出。
3. `src/ExcelCalibrationAddin.Vsto/TemplateLibraryManagerDialog.cs`、`RandomGenerationConfigurationDialog.cs` 和任务窗格相关文件偏大，后续 UI 状态应进一步下沉。
4. 本地服务 `Program.cs` 是轻量实现，继续扩展前需要先处理停止、端口占用和请求并发策略。
5. VSTO 旧式项目格式无法被部分 `dotnet` 工具完整分析，需要人工维护 Office/VSTO 引用和运行时兼容性。

## 9. 项目审查结论与验证结果

### 9.1 审查范围

2026-07-26 审查覆盖：

- Contracts、Core、Host、Vsto、LocalServer 和测试项目。
- README、功能需求规格、重构路线、构建与安装脚本。
- 模板识别、规则生成、SQLite 持久化、HTTP 同步、诊断日志和 Excel 互操作边界。
- 审查开始前工作区已有的未提交修改；处理过程中未回退或覆盖这些修改。

### 9.2 已修复问题

1. 修复 Excel 快照捕获过程中的 4 处乱码进度文案，并完成源码乱码特征复扫。
2. LocalServer 限制为 HTTP 回环地址，防止配置错误时监听局域网或公网地址。
3. 移除宽松 CORS，拒绝带 `Origin` 的浏览器请求，降低网页跨站修改本地模板库的风险。
4. POST 接口强制 `application/json`，缺少模板指纹等请求返回明确的 400 错误。
5. 请求体按实际读取字节限制为 5 MB，分块传输不能再绕过大小限制。
6. 服务端内部异常不再原样返回客户端，避免泄露 SQLite 路径、堆栈和运行环境信息。
7. SQLite 连接串改用 `SQLiteConnectionStringBuilder`，合法路径中的分号不会再被解释为连接参数。
8. 空后端地址、API 前缀和 SQLite 路径自动恢复为安全默认值。
9. 删除两套已被当前同步和本地匹配流程替代、全项目零引用的远端目录模糊匹配实现。
10. 删除零引用的旧单值输入、旧状态下拉框、旧同步保存入口及相关辅助函数。
11. 删除无效配置自赋值，并清理受影响文件中的无用引用。
12. 新增空配置恢复和 SQLite 分号路径回归测试。

### 9.3 自动化与构建验证

执行结果：

1. `powershell -ExecutionPolicy Bypass -File .\tools\verify.ps1`：Restore 和 Debug 完整解决方案 Build 成功，0 警告、0 错误。
2. Release 完整解决方案 Build 成功；Contracts、Core、Host、Vsto、LocalServer 和测试程序集均成功生成。
3. `dotnet test .\tests\ExcelCalibrationAddin.Core.Tests\ExcelCalibrationAddin.Core.Tests.csproj --configuration Debug`：110/110 通过，0 跳过、0 失败。
4. Core、Host、LocalServer 和测试项目的 NuGet 漏洞扫描未发现已知易受攻击包；Contracts 无第三方包。
5. 旧式 VSTO 项目格式不受 `dotnet list package --vulnerable` 支持，其 Office/VSTO 直接引用仍需人工核对。
6. `git diff --check` 通过，未发现空白错误。
7. 私有/内部方法零引用复扫仅保留程序入口 `Main`。

### 9.4 本地服务真实 HTTP 验证

已验证：

- `GET /health` 正常返回。
- 带浏览器 `Origin` 的请求被拒绝。
- 非 `application/json` POST 被拒绝。
- 缺少模板指纹的匹配请求被拒绝。
- 超过 5 MB 的分块请求被拒绝，不能绕过大小限制。
- 合法但未命中模板的匹配请求保持正常响应。

### 9.5 Excel 文件级实机验证

已生成 `outputs\excel-regression\Excel加载项回归样本.xlsx`，并在桌面版 Excel 中完成文件级验证：

- 工作簿包含回归清单、原始记录、公式分支和无关台账 4 张工作表。
- 能正常打开、完整重算、保存和关闭。
- 原始记录打印区域为 `$A$1:$H$14`。
- 相对/绝对误差两个公式分支均返回“合格”。
- 关闭验证实例后没有残留 Excel 进程。

以上仅证明回归资产和 Excel 文件行为正常，不代表 VSTO 加载项交互全流程已经通过。

### 9.6 尚未完成的验证与剩余风险

1. 尚未在真实 Excel 中完整执行打开、识别、编辑、保存、生成、撤销和关闭流程。
2. 多工作簿切换时的状态隔离、COM 生命周期和任务窗格事件只能通过实机最终确认。
3. 打印区域、合并单元格、多级表头、公式显示值和异常回滚仍需要代表性工作簿覆盖。
4. 真实远端模板服务的网络失败、冲突解决和 pending upload 恢复尚需联调。
5. LocalServer 仍是单线程串行模型，适合当前单机加载项，不适合作为多用户服务。
6. 测试主要集中在一个大文件中，继续增加测试前应先按领域整理结构。

## 10. 项目核心思路提炼

### 10.1 以快照隔离 Excel COM

Excel COM 不应出现在核心业务逻辑中。当前项目的正确方向是先用 `ExcelInteropSnapshotProvider` 把工作簿转换为 `WorkbookSnapshot`，之后的指纹、识别、规则构建和生成逻辑都基于纯模型运行。

收益：

- 降低 COM 生命周期和线程问题对业务逻辑的污染。
- 让核心逻辑可以用单元测试覆盖。
- 便于后续增加 OpenXML、样本文件或其他导入方式。

### 10.2 以模板规则沉淀业务经验

用户每次在 Excel 中完成的区域选择、单位判断、公式解析和误差参数，本质上都是模板知识。项目的主线不是“一次性生成”，而是把这些知识固化为 `MeasurementRule` 和 `TemplateFieldDefinition`，后续靠指纹匹配复用。

收益：

- 同类模板下次自动匹配。
- 可做模板版本管理和远端同步。
- 能把复杂 Excel 结构转化为稳定的规则资产。

### 10.3 以参数解析驱动生成，而不是直接填随机数

生成测量值之前必须先解析标准值、技术要求、允许误差、量程、公式关系和数字格式。随机数生成只是最后一步，前面的解析质量决定结果是否可信。

收益：

- 能处理相对误差、绝对误差、引用量程、最大误差、重复性等不同规则。
- 能用公式回代验证生成值是否满足 Excel 结果列。
- 能避免生成看似随机但业务上不合格的数据。

### 10.4 本地优先，远端增强

模板匹配优先查本地 SQLite。远端服务用于同步、补全和共享模板；远端不可用时，本地缓存和 pending upload 保证用户工作不断档。

收益：

- Excel 插件启动和生成速度更稳定。
- 网络失败不阻断本地工作。
- 后续可以逐步扩展团队模板库。

### 10.5 真实 Excel 回归不可替代

Core 和 Host 的纯逻辑可以大量自动化，但 VSTO、COM、任务窗格、Ribbon、撤销、工作簿切换和关闭释放必须在真实 Excel 中验证。

收益：

- 避免自动化测试通过但实际 Excel 事件、状态和 COM 对象失败。
- 能发现打印区域、合并单元格、公式显示值等 Excel 特有问题。

## 11. 推荐后续开发顺序

### 阶段 1：真实 Excel 回归

使用代表性工作簿覆盖：

1. 单工作簿和多工作簿切换。
2. 打印区域。
3. 合并单元格。
4. 多级表头。
5. 公式列和条件公式。
6. 多个校准项目。
7. 模板保存、重新打开、再次匹配。
8. 生成、撤销、异常回滚和关闭 Excel。

交付物：

- 手工回归清单：已提供 `Excel实机回归清单.md`。
- 代表性工作簿：已提供 `outputs\excel-regression\Excel加载项回归样本.xlsx`，并通过真实 Excel 文件级打开、重算、打印区域和关闭验证。
- 对应日志和问题编号。

当前状态：回归资产已完成；8 个 VSTO 加载项交互用例仍需在可见 Excel 会话中逐项执行并记录结果。

### 阶段 2：把实机缺陷转为测试

凡是能脱离 COM 表达的问题，都转成 Core 或 Host 测试。COM 特有问题至少沉淀为稳定手工步骤和诊断日志字段。

优先补测试：

1. 预览生成工作流。
2. 模板字段定义匹配。
3. 公式条件分支。
4. 合并单元格行映射。
5. 保存模板时瞬时值剥离。

当前状态：上述领域已有自动化覆盖；本轮新增 `PreviewWorkflowTests.cs`，覆盖有/无误差公式的预览警告以及警告去重，测试基线增至 110 项。

### 阶段 3：拆分生成预览工作流

从 `GenerateMeasurementUseCase.PreviewWorkflow.cs` 中逐步提取：

1. 标准值映射。
2. 误差预览。
3. 公式分支解析。
4. 行映射预处理。
5. 警告消息聚合。

原则：

- 每次只移动一组职责。
- 不改变行为。
- 每次拆分后运行完整测试和解决方案构建。

当前状态：已将格式/分辨力处理和趋势误差处理分别拆到 `GenerateMeasurementUseCase.PreviewFormatting.cs` 与 `GenerateMeasurementUseCase.TrendErrors.cs`，主预览文件由 1014 行降至 773 行，行为测试保持通过。标准值映射、公式分支和消息聚合仍待继续提取。

### 阶段 4：整理测试结构

将 `CoreBehaviorTests.cs` 按领域拆分：

```text
FingerprintTests.cs
TemplateDefinitionTests.cs
GenerationRuleTests.cs
ParameterResolverTests.cs
TemplateCacheTests.cs
TemplateSyncTests.cs
FormulaVerifierTests.cs
RowMappingTests.cs
```

原则：

- 先移动测试，不改断言。
- 测试数量保持一致。
- 拆完再考虑测试夹具和样本数据复用。

### 阶段 5：治理 UI 状态和本地服务生命周期

UI 方向：

- 将规则编辑状态、模板保存状态和生成按钮可用性从 WinForms 控件中提取为可测试模型。
- 任务窗格只负责展示和事件转发。

本地服务方向：

- 增加端口占用提示。
- 增加停止信号和取消令牌。
- 明确单实例策略。
- 如需多客户端，再引入正式宿主和并发模型。

## 12. 开发约束

1. 不把业务判断写进 UI 事件。
2. 不让 Core 依赖 Excel COM、WinForms 或 MessageBox。
3. 保存模板前必须校验指纹、规则、区域、误差参数和模板身份。
4. 生成前必须先解析参数并校验规则完整性。
5. 写 Excel 时优先批量读写，避免循环中频繁访问 COM。
6. 异常必须记录原因，不能空 `catch` 后静默失败。
7. 每次改动只解决一个明确问题，避免无关重构。
8. 涉及共享逻辑时必须补测试或运行覆盖范围足够的既有测试。
9. 不在代码或日志中写入密钥、令牌、密码和私有配置。
10. 本地服务默认只服务单机加载项，不按公网服务标准扩展，除非需求明确变化。

## 13. 交付完成标准

一个功能或修复完成前，应满足：

1. 调用链已读清楚。
2. 改动范围清晰且符合现有分层。
3. 正常路径、异常路径和边界条件已考虑。
4. 自动化测试、构建或真实 Excel 回归已至少覆盖相关风险。
5. 文档或注释只补必要信息，不制造重复说明。
6. 未回退或覆盖用户已有修改。
7. 最终说明明确写出改了什么、验证了什么、剩余风险是什么。
