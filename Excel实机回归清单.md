# Excel 加载项实机回归清单

> 适用版本：2026-07-26 及之后。执行前先运行 `tools\verify.ps1`，确保自动化基线通过。

## 1. 回归资产

- 代表性样本：`outputs\excel-regression\Excel加载项回归样本.xlsx`
- 生成脚本：`tools\create_excel_regression_workbook.mjs`
- 日志目录：`%LOCALAPPDATA%\ExcelCalibrationAddin\logs`

样本包含四张工作表：回归清单、原始记录、公式分支和应被自动匹配跳过的台账。原始记录覆盖合并表头、多个校准项目、公式列和技术要求；公式分支覆盖相对误差与绝对误差条件分支。

## 2. 执行要求

1. 每次执行前复制一份样本，禁止直接覆盖基线文件。
2. 记录 Excel 版本、加载项版本、Windows 版本和测试时间。
3. 每个失败必须保留失败工作簿、对应日志和最小复现步骤。
4. 涉及多工作簿切换时，另开一个空白工作簿，不得复用同一工作簿中的工作表切换代替。
5. 测试异常回滚前先保存副本，避免丢失人工填写结果。

## 3. 必测用例

| 编号 | 场景 | 核心检查点 |
| --- | --- | --- |
| EXCEL-01 | 单工作簿全流程 | 识别、编辑、保存模板、生成、撤销均可完成，撤销恢复原值。 |
| EXCEL-02 | 多工作簿切换 | 任务窗格、规则和生成状态按工作簿隔离。 |
| EXCEL-03 | 打印区域 | 将原始记录设为 `A1:H14` 打印区域后，识别不读取区域外内容。 |
| EXCEL-04 | 合并单元格和多级表头 | 表头层级正确，合并覆盖行不被识别为可写测量行。 |
| EXCEL-05 | 条件公式分支 | 标准值 50 使用相对误差分支，标准值 200 使用绝对误差分支。 |
| EXCEL-06 | 多个校准项目 | 示值误差和重复性写入独立区域，不覆盖标准值、技术要求和公式。 |
| EXCEL-07 | 模板重新匹配 | 保存、关闭、重新打开后本地模板命中；瞬时测量值未固化到规则。 |
| EXCEL-08 | 异常回滚和关闭 | 失败时无部分写入，关闭后无残留 Excel 进程或 COM 异常。 |

## 4. 问题记录最小字段

每个问题至少记录：

- 问题编号和用例编号。
- 工作簿名、工作表名和模板指纹。
- 预期结果、实际结果和稳定复现步骤。
- 日志文件及关键时间点。
- 是否能脱离 COM 转成 Host/Core 自动化测试。

## 5. 完成标准

1. 8 个用例全部执行并记录状态。
2. 所有失败均有问题编号和复现资产。
3. 可脱离 COM 的缺陷已补失败测试；COM 特有问题已有稳定手工步骤和诊断日志。
4. 阻断级问题关闭后，重新执行完整清单和自动化验证。
